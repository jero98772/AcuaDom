#include <Arduino.h>
/*void recivemsg(){
  while (Serial.available() == 0) {
    String dat=Serial.readString();
    if(dat!=""){
      Serial.println(dat);
    }
  }*/
void setup() {
  
}

void loop() {
  // put your main code here, to run repeatedly:
}